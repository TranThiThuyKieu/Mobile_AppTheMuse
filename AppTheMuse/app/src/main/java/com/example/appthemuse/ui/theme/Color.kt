package com.example.appthemuse.ui.theme

import androidx.compose.ui.graphics.Color

// Bảng màu chuẩn thương hiệu The Muse (Chủ đạo: Indigo / Blue)
val PrimaryIndigo = Color(0xFF4F46E5)      // Màu xanh tím chủ đạo của nút bấm chính
val PrimaryDark = Color(0xFF3730A3)        // Màu chủ đạo khi nhấn giữ / Dark mode

val SecondaryGray = Color(0xFF6B7280)      // Màu xám phụ cho text mờ, icon phụ
val BackgroundLight = Color(0xFFF9FAFB)    // Nền trắng xám dịu mắt cho Light mode
val BackgroundDark = Color(0xFF111827)     // Nền đen xám sâu cho Dark mode

// Màu sắc bổ trợ cho các trạng thái lưới (Grid tuyển chọn thể loại)
val SurfaceVariantLight = Color(0xFFF3F4F6) // Thẻ thể loại chưa chọn (Light)
val SurfaceVariantDark = Color(0xFF1F2937)  // Thẻ thể loại chưa chọn (Dark)
val PrimaryContainerLight = Color(0xFFEEF2FF)// Thẻ đã chọn (Light)
val PrimaryContainerDark = Color(0xFF312E81) // Thẻ đã chọn (Dark)

val GoldStar = Color(0xFFFFD700) // Màu vàng tươi dùng cho các biểu tượng đánh giá (Review Star)
val SuccessGreen = Color(0xFF4CAF50) // Màu xanh lá báo trạng thái thành công (ví dụ: Đã tải xuống)